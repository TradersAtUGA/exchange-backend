 name: cpp tests

 on: [push, pull_request]

 jobs:
   test-cpp:
     runs-on: ubuntu-latest

     steps:
       - uses: actions/checkout@v3

       - name: Set Up Go
         uses: aminya/setup-cpp@v5
         with:
           compiler: gcc
           cmake: true

       